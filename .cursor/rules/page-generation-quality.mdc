---
description: 页面生成质量闸门，确保组件选型和设计还原一致
globs: "**/src/pages/**/*.tsx"
alwaysApply: false
---

# 页面生成质量闸门

每次创建或大改页面时，按以下顺序执行并在代码中落实：

1. 先搭结构：页头、布局容器、滚动区域
2. 再放内容：业务组件和信息层级
3. 再补状态：loading / empty / error
4. 最后优化：交互细节与边界行为

## 最低通过标准

- 至少使用 3 个 `@/components/wuhan/composed/*` 组件
- 不出现外部 UI 库导入
- 不出现硬编码色值
- 组件命名、文件命名、导入路径符合项目约定

## 交付前自检

- [ ] 页面是否能和设计稿在布局结构上逐块对齐
- [ ] 主要操作路径是否完整（创建、筛选、确认、返回）
- [ ] 文案层级与视觉重点是否匹配
- [ ] 是否存在多余样式覆盖导致组件失真

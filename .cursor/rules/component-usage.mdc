---
description: 组件库使用规范，生成页面时必须基于已有组件
globs: "**/*.tsx"
alwaysApply: false
---

# 组件使用规范

## 核心原则

**禁止从零手写 UI 元素**。所有页面必须基于已有组件库构建。

页面生成时必须遵循：`composed > blocks > ui > 自定义容器样式`。

## 生成流程（必须执行）

1. **先定骨架**：先选 `PageHeader` / `Sidebar` / `TripleSplitPane` 等布局组件。
2. **再填业务区**：优先选择对应业务 composed 组件（消息、表单、卡片、任务、上传等）。
3. **最后补交互状态**：每页至少实现 `normal + loading + empty/error` 三类状态。
4. **补基础原语**：仅当 composed 不满足时，才落到 blocks/ui。

## 还原度要求（对照设计稿）

- 布局层级保持一致（栏位数量、主次关系、滚动容器边界）
- 文案层级保持一致（标题/正文/辅助文案）
- 交互入口保持一致（主要按钮、筛选、输入、反馈动作）
- 不擅自新增视觉风格，不引入新 UI 体系

## 组件导入路径

```tsx
// ✅ 优先使用 composed 组件
import { PageHeader } from "@/components/wuhan/composed/page-header";
import { ComposedSender } from "@/components/wuhan/composed/sender";
import { AIMessage, UserMessage } from "@/components/wuhan/composed/message";

// ✅ 需要更底层控制时使用 blocks
import { SenderTextarea, SenderContainer } from "@/components/wuhan/blocks/sender-01";

// ✅ 基础 UI 使用 shadcn
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
```

## 可用 composed 组件

| 组件 | 文件 | 用途 |
|------|------|------|
| PageHeader | page-header.tsx | 页头（logo + 标题 + 操作区） |
| TripleSplitPane | triple-split-pane.tsx | 三栏可折叠布局 |
| ComposedSender | sender.tsx | 消息输入框（附件/模式/发送） |
| ResponsiveSender | responsive-sender.tsx | 响应式输入框 |
| AIMessage / UserMessage | message.tsx | AI/用户消息气泡 |
| MessageList | message-list.tsx | 消息列表 |
| MessageFeedbackActions | message-feedback.tsx | 消息反馈操作 |
| Sidebar | sidebar.tsx | 侧边栏导航 |
| Avatar / AvatarHeader | avatar.tsx / avatar-header.tsx | 头像与头像组 |
| BlockButton | block-button.tsx | 多变体按钮 |
| BlockInput | block-input.tsx | 输入框 |
| BlockSelect | block-select.tsx | 下拉选择 |
| BlockAccordion | block-accordion.tsx | 手风琴折叠 |
| Checkbox / Radio | checkbox.tsx / radio.tsx | 选择控件 |
| Progress | progress.tsx | 进度条/环形进度 |
| StatusTag / Tag | status-tag.tsx / tag.tsx | 状态标签 |
| Tooltip | tooltip.tsx | 提示气泡 |
| Divider | divider.tsx | 分割线 |
| IconButton | icon-button.tsx | 图标按钮 |
| ToggleButton | toggle-button.tsx | 切换按钮 |
| FileCard / DocumentCard | file-card.tsx / document-card.tsx | 文件/文档卡片 |
| AgentCard | agent-card.tsx | AI Agent 卡片 |
| TaskCard / TaskList | task-card.tsx / task-list.tsx | 任务卡片/列表 |
| GoalCard | goal-card.tsx | 目标卡片 |
| ReportCard | report-card.tsx | 报告卡片 |
| SelectCard | select-card.tsx | 可选卡片 |
| Welcome | welcome.tsx | 欢迎界面 |
| Prompt | prompt.tsx | 提示词面板 |
| QuickAction | quick-action.tsx | 快捷操作 |
| Suggestion | suggestion.tsx | 建议面板 |
| DeepThinking | deep-thinking.tsx | 深度思考展示 |
| ThinkingProcess | thinking-process.tsx | 思考过程步骤 |
| ExecutionResult | execution-result.tsx | 执行结果 |
| Feedback | feedback.tsx | 反馈组件 |
| Upload | upload.tsx | 上传组件 |
| DynamicForm | dynamic-form.tsx | 动态表单 |
| ConfirmPanel | confirm-panel.tsx | 确认面板 |
| HistoryItem | history-item.tsx | 历史记录项 |
| Markdown | markdown/index.tsx | Markdown 渲染器 |

## 可用 shadcn UI 组件

Button, Collapsible, Field, Input, Label, Popover, Resizable, Select, Separator, Slider, Switch, Tabs, Textarea

## 图标

使用 `lucide-react`，不要引入其他图标库。

```tsx
import { Search, Settings, User, Plus } from "lucide-react";
```

## 禁止项

- 禁止引入 `@mui/*`、`antd`、`chakra-ui` 等外部 UI 组件库
- 禁止用纯 `div + class` 重造按钮、输入框、选择器、弹层等基础组件
- 禁止用相对路径深层跨目录导入组件（统一使用 `@/` 别名）
